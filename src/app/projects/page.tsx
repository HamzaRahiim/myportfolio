import Projct from "@/components/sections/project/Projct";
export const revalidate = 172800;

const page = () => {
  return (
    <section>
      <Projct />
    </section>
  );
};
export default page;
