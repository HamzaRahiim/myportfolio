import Projct from "@/components/sections/project/Projct";
export const revalidate = 86400;

const page = () => {
  return (
    <section>
      <Projct />
    </section>
  );
};
export default page;
